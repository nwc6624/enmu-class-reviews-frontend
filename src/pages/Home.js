import React from 'react';
import NavBar from '../components/NavBar';

const Home = () => {
    return (
        <div>
            <NavBar />
            <h1>Welcome to ENMU Class Reviews</h1>
            <p>This is the home page where users can find information about the platform.</p>
        </div>
    );
};

export default Home;
